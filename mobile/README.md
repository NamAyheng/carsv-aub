# CarSV mobile

React Native app for the CarSV garage. It uses the same Django API as the website.

```powershell
npm install
npx expo start
```

Scan the QR code with Expo Go. Keep `backend` running on port 8000.
