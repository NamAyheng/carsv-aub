# Convert Existing HTML CSS JavaScript Frontend to React + Django

## Objective

Convert the existing **HTML + CSS + JavaScript** project into a **React + Django** project.

At this stage, the project is **static**.

There is **no real data, database, API, or backend functionality yet**.

The goal is only to establish the new project structure and convert the existing frontend into React while preparing Django as the backend foundation for future development.

---

## 1. Current Project

The existing project uses:

- HTML
- CSS
- JavaScript

The current UI and pages are already designed.

**Do not redesign the application.**

---

## 2. Target Project

Convert the project into:

```text
Frontend
React

Backend
Django
```

# Note

also create a new prompt file for the result something like:

- How to run

- How to check the api or result (for future use)

- how to run dashboard or admin

- if this is username or password, please note everything down

make sure there is a single files that tell use how to use this project.
